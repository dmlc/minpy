sudo: required

dist: trusty

before_install:
  - "sudo apt-get update -qq"
  - "sudo apt-get install --yes libblas-dev liblapack-dev gfortran"

language: python

python:
  - "3.5"

script:
  - "echo \"No tests yet\""

